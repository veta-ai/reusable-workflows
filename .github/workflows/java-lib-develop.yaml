name: CI - Snapshot Deploy (Java Library)

on:
  workflow_call:
    inputs:
      java-version:
        required: false
        type: string
        default: '21'
      sonar_project_key:
        required: true
        type: string

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Build & Test
    # if: startsWith(github.ref, 'refs/heads/develop/')
    uses: ./.github/workflows/build-java-maven.yaml
    with:
      java-version: ${{ inputs.java-version }}
      sonar_project_key: ${{ inputs.sonar_project_key }}

  deploy-snapshot:
    name: Deploy -SNAPSHOT to Nexus
    needs: build
    uses: ./.github/workflows/deploy-java-maven-snapshot.yaml
    secrets: inherit
    with:
      java-version: ${{ inputs.java-version }}